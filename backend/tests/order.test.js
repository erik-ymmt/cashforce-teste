const { expect } = require('chai');
// const sinon = require('sinon');

describe('Order table endpoints tests', async () => {
  describe('Find orders tests', async () => {
    it('tests', async () => {
      expect(1).to.be.equal(10);
    });
  });
});
